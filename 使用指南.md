# 古钱币收藏小程序 - 使用指南

## 快速开始

### 第一步：导入项目

1. 打开 **微信开发者工具**
2. 点击 **"+"** 或 **"导入项目"**
3. 项目目录选择：`e:\myapp\ancient-coin-collection`
4. AppID 选择：
   - 有 AppID：填写你的小程序 AppID
   - 无 AppID：选择 **"测试号"** 或 **"小程序官方代码演示"**
5. 点击 **"确定"**

### 第二步：配置项目

打开 `project.config.json` 文件，找到以下配置项：

```json
{
  "appid": "wxxxxxxxxxxx"
}
```

将 `appid` 修改为你自己的小程序 AppID（如果没有，可先使用测试号）

### 第三步：运行项目

1. 点击开发者工具顶部的 **"编译"** 按钮
2. 在模拟器中查看小程序效果
3. 可以使用 **"预览"** 功能在真机上测试

## 功能使用指南

### 📱 收藏第一个版别

1. **进入分类页面**
   - 点击底部导航栏的 **"分类"**

2. **选择分类**
   - 点击任意分类卡片（如"明清货币"）

3. **选择年代**
   - 点击年代列表中的年代（如"清朝"）

4. **收藏版别**
   - 在版别列表中找到要收藏的版别
   - 点击 **"收藏并上传"** 按钮

5. **上传图片**
   - 点击 **"选择图片"** 按钮
   - 从相册选择或拍照
   - 可选择图片类型（正面/背面/其他）
   - 最多可上传 9 张图片

6. **添加备注**（可选）
   - 在备注框中输入说明文字

7. **保存**
   - 点击 **"保存"** 按钮完成收藏

### 📊 查看收藏统计

1. 点击底部导航栏的 **"统计"**
2. 查看以下数据：
   - **总体概况**: 总版别数、已收藏数、图片数、完成度
   - **按分类统计**: 每个分类的收藏进度
   - **按年代统计**: 每个年代的详细数据
   - **最近收藏**: 最近收藏的版别列表

### 👤 管理个人收藏

1. 点击底部导航栏的 **"我的"**
2. 可以执行以下操作：
   - **查看收藏**: 浏览所有收藏的版别
   - **预览图片**: 点击图片查看大图
   - **删除收藏**: 删除不需要的收藏记录
   - **导出数据**: 导出收藏清单到剪贴板
   - **清空数据**: 清空所有收藏数据

## 数据结构说明

### 默认分类体系

小程序内置了以下分类:

```
先秦货币
├── 战国 (公元前 475-221 年)
│   ├── 刀币
│   ├── 布币
│   ├── 圜钱
│   └── 蚁鼻钱
└── 秦朝 (公元前 221-207 年)
    ├── 半两
    └── 秦半两

汉唐货币
├── 汉朝 (公元前 202-公元 220 年)
│   ├── 五铢
│   ├── 货泉
│   └── 大泉五十
└── 唐朝 (618-907 年)
    ├── 开元通宝
    └── 乾元重宝

宋元货币
├── 宋朝 (960-1279 年)
│   ├── 宋元通宝
│   ├── 太平通宝
│   ├── 淳化元宝
│   └── 至道元宝
└── 元朝 (1271-1368 年)
    ├── 大元通宝
    └── 至大通宝

明清货币
├── 明朝 (1368-1644 年)
│   ├── 洪武通宝
│   ├── 永乐通宝
│   └── 宣德通宝
└── 清朝 (1644-1912 年)
    ├── 顺治通宝
    ├── 康熙通宝
    ├── 雍正通宝
    ├── 乾隆通宝
    └── 嘉庆通宝

民国货币
└── 民国 (1912-1949 年)
    ├── 开国纪念币
    ├── 袁大头
    └── 孙小头
```

### 版别细分

每个版别还细分为:
- 小平
- 折二
- 折三
- 当十

## 自定义配置

### 修改分类数据

编辑 `app.js` 文件中的 `initDefaultCategories()` 方法:

```javascript
initDefaultCategories() {
  const defaultCategories = [
    {
      id: 1,
      name: '你的分类名称',
      icon: '/images/your-icon.png',
      eras: [
        { id: 101, name: '年代名称', years: '年代时间' }
      ]
    }
  ];
  
  wx.setStorageSync('coinCategories', defaultCategories);
}
```

### 修改版别列表

编辑 `app.js` 文件中的 `getEraVariants()` 方法:

```javascript
getEraVariants(eraId, eraName, startId) {
  const variantTemplates = {
    101: ['版别 1', '版别 2', '版别 3']
  };
  
  // ... 其余代码
}
```

### 修改主题颜色

编辑 `app.wxss` 和各页面的 `.wxss` 文件:

```css
/* 主色调 */
.btn-primary {
  background-color: #8B4513; /* 修改为你喜欢的颜色 */
}

.section-title {
  color: #8B4513; /* 修改标题颜色 */
}
```

## 常见问题

### Q1: 图片无法上传？
**A:** 检查以下几点:
- 确保已授权相机和相册权限
- 图片格式应为 jpg、png 等常见格式
- 图片大小不要过大（建议<5MB）
- 当前版本使用本地临时路径，重启小程序后图片可能丢失

### Q2: 数据会丢失吗？
**A:** 
- 数据存储在本地（wx.Storage）
- 卸载小程序或清除缓存会导致数据丢失
- 建议定期使用"导出数据"功能备份
- 生产环境建议接入云开发数据库

### Q3: 如何添加更多版别？
**A:** 
- 编辑 `app.js` 中的 `getEraVariants()` 方法
- 在对应的年代数组中添加版别名称
- 重新编译小程序

### Q4: TabBar 图标不显示？
**A:** 
- 检查 `images/` 目录下是否有对应的图标文件
- 图标路径在 `app.json` 中配置
- 可以暂时使用 emoji 图标替代

### Q5: 如何在真机上测试？
**A:** 
1. 在微信开发者工具中点击 **"预览"**
2. 使用微信扫描二维码
3. 即可在真机上体验小程序

## 开发建议

### 1. 数据持久化
当前版本使用本地存储，建议后续接入:
- 微信云开发数据库
- 自建后端 API
- 第三方云存储服务

### 2. 图片管理
- 使用云存储保存图片
- 添加图片压缩功能
- 支持图片裁剪和编辑

### 3. 用户体验优化
- 添加下拉刷新
- 添加上拉加载更多
- 优化加载速度
- 添加骨架屏

### 4. 功能扩展
- 添加搜索功能
- 支持版别详情编辑
- 添加分享功能
- 支持批量操作

## 技术支持

### 微信开发者工具
- 官方文档：https://developers.weixin.qq.com/miniprogram/dev/framework/
- 开发者社区：https://developers.weixin.qq.com/community/

### 常见问题排查
1. 编译失败：检查 JSON 配置文件格式
2. 页面不显示：检查页面路径配置
3. 样式不生效：检查 WXSS 文件引用
4. 数据不保存：检查 Storage 操作是否正确

## 更新日志

### v1.0.0 (2026-04-27)
- ✅ 基础分类功能
- ✅ 版别管理
- ✅ 图片上传
- ✅ 统计功能
- ✅ 个人中心

---

**祝你使用愉快！** 🎉
